---
title: Arquitetura
---

Como qualquer compilador, o YAMLResume também contém um pipeline que
transforma os dados do currículo desde a entrada até à saída com
múltiplas fases.

- **Fase de Entrada**: aceita ficheiros de currículo YAML e JSON como entrada.
- **Fase de Análise**: analisa e valida o conteúdo e layout do currículo
  desde os ficheiros de entrada.
- **Fase de Transformação**: transforma os dados de conteúdo e layout do
  currículo com um pipeline.
- **Fase de Geração de Código**: renderiza os dados de conteúdo e layout do
  currículo usando modelos LaTeX, HTML ou Markdown.
- **Fase de Saída**: gera os documentos finais PDF, HTML ou Markdown.

Aqui está um diagrama de arquitetura de alto nível do YAMLResume a 30000
pés:

<Mermaid chart="
  flowchart TD
    subgraph A[Fase de Entrada]
      A1[Ficheiro de Currículo YAML]
      A2[Ficheiro de Currículo JSON]
    end

    subgraph B[Fase de Análise]
      B1[yaml.parse]
      B2[validação de esquema]
      B3[MarkdownParser]
    end

    subgraph C[Fase de Transformação]
      C1[processamento escapeLatex/escapeHtml]
      C2[localização de data]
      C3[transformação de URL]
      C4[transformação de localização]
      C5[...]
    end

    subgraph D[Fase de Geração de Código]
      subgraph D1[Motor HTML]
        D1a[Renderizador de modelos]
        D1b[calm]
        D1c[vscode]
        D1d[Documento HTML]
      end
      subgraph D2[Motor LaTeX]
        D2a[Renderizador de modelos]
        D2b[moderncv-banking]
        D2c[moderncv-classic]
        D2d[moderncv-casual]
        D2e[Documento LaTeX]
      end
      subgraph D3[Motor Markdown]
        D3a[Renderizador de modelos]
        D3b[default]
        D3c[Documento Markdown]
      end
    end

    subgraph E[Fase de Saída]
      E1[xetex -> resume.pdf]
      E2[tectonic -> resume.pdf]
      E3[HTML -> resume.html]
      E4[Markdown -> resume.md]
    end

    A1 --> B1
    A2 --> B1
    B1 --> B2
    B2 --> B3
    B3 --> C1
    C1 --> C2
    C2 --> C3
    C3 --> C4
    C4 --> C5
    C5 --> D1a
    C5 --> D2a
    C5 --> D3a
    D1a --> D1b
    D1a --> D1c
    D1b --> D1d
    D1c --> D1d
    D2a --> D2b
    D2a --> D2c
    D2a --> D2d
    D2b --> D2e
    D2c --> D2e
    D2d --> D2e
    D3a --> D3b
    D3b --> D3c
    D1d --> E3
    D2e --> E1
    D2e --> E2
    D3c --> E4

"/>

## Pipeline de Processamento de Dados

O pipeline de transformação é responsável por processar os dados brutos do
currículo desde ficheiros YAML e prepará-los para a renderização. Opera em
duas fases principais: transformação de conteúdo e transformação de layout:

- A fase de transformação de conteúdo processa todos os dados textuais,
  aplica regras de localização, escapa caracteres especiais de LaTeX e computa
  valores derivados.
- A fase de transformação de layout funde a configuração de layout
  fornecida pelo utilizador com valores predefinidos e aplica regras de
  tipografia específicas do idioma local.

<Mermaid
  chart="
flowchart TD
  A[Dados Brutos do Currículo] --> B[transformResume]
  B --> B1[transformResumeContent]
  B1 --> B2[transformEducationCourse]
  B2 --> B3[transformEducationDegree]
  B3 --> B4[transformDate]
  B4 --> B5[transformEndDate]
  B5 --> B6[transformKeywords]
  B6 --> B7[...]
  B --> C1[transformResumeLayout]
  C1 --> C2[definir valores predefinidos]
  C2 --> C3[definir ajustes locais]
  B7 --> D[Currículo Transformado]
  C3 --> D
  D --> D1[Ready for Code Generation]
"
/>

Para um pipeline de processamento de dados longo como este, o código fala mais
do que as palavras, podes consultar
[transform.ts](https://github.com/yamlresume/yamlresume/blob/main/packages/core/src/preprocess/transform.ts)
para detalhes se estiveres interessado na implementação.
