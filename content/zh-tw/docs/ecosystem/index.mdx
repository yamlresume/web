---
title: 生態系統
---

YAMLResume 提供了一套可復用的套件和工具，用於構建履歷工作流程、嵌入編輯器、自動化構建、管理範例履歷以及轉換現有的履歷資料。

## @yamlresume/node

[`@yamlresume/node`](./ecosystem/node) 是 YAMLResume CLI 背後的可復用 Node.js 執行時。

- 讀取並驗證 YAML、YML 或 JSON 履歷檔案，並提供帶位置的錯誤資訊。
- 構建已配置的佈局、監視檔案，並編譯 LaTeX 或 Typst PDF。
- 從範例建立履歷，或使用 AI 生成和翻譯檔案。

## @yamlresume/playground

[`@yamlresume/playground`](./ecosystem/playground) 是官方[遊樂場](/zh-tw/playground)背後的 React 元件。

- 嵌入架構感知的 Monaco YAML 編輯器和即時多佈局預覽。
- 使用響應式的桌面和行動端佈局、匯出操作和本地化 UI 標籤。
- 與 Vite、Next.js、Tailwind CSS 以及自訂 Monaco worker 設定整合。

## create-yamlresume

[create-yamlresume](./ecosystem/create-yamlresume) 讓您可以通過一行命令輕鬆啟動新的 YAMLResume 專案。它將搭建您的專案目錄，安裝必要的依賴，並生成示例履歷文件，這樣您就可以立即開始。

- 使用 `npx create-yamlresume my-resume` 或類似的 `npm`、`yarn` 或 `pnpm` 命令搭建新專案。
- 包括即用型專案結構、用於構建和驗證履歷的腳本，以及示例 YAML 履歷文件。

## json2yamlresume

[json2yamlresume](./ecosystem/json2yamlresume) 是一個用於將 [JSON Resume](https://jsonresume.org/) 文件轉換為 YAMLResume 格式的 CLI 工具。

- 將 JSON Resume 文件轉換為 YAMLResume，根據需要轉換欄位和結構。
- 確保您的數據根據 YAMLResume 模式進行驗證。

這些工具幫助您從其他格式遷移，自動化專案設置，並簡化您與 YAMLResume 的工作流程。

## samples

[`@yamlresume/samples`](./ecosystem/samples)
提供了一系列針對常見職位的精選範例履歷目錄，每一份都翻譯成了 12 種語言。

- 為 `yamlresume new --sample` 指令和 `yamlresume samples` list 子指令提供支援。
- 提供查詢 API，可依語言、類別或標籤篩選範例履歷。

## 了解更多

<Cards>
  <Card
    href="./ecosystem/node"
    title="@yamlresume/node"
    description="在 Node.js 中讀取、驗證、構建、監視、生成和翻譯履歷檔案"
  />
  <Card
    href="./ecosystem/playground"
    title="@yamlresume/playground"
    description="在 React 應用中嵌入 YAMLResume 的架構感知編輯器和即時預覽"
  />
  <Card
    href="./ecosystem/create-yamlresume"
    title="create-yamlresume"
    description="使用一行命令創建新的 YAMLResume 專案"
  />
  <Card
    href="./ecosystem/json2yamlresume"
    title="json2yamlresume"
    description="將 JSON Resume 轉換為 YAMLResume"
  />
  <Card
    href="./ecosystem/samples"
    title="samples"
    description="12 種語言的精選範例履歷"
  />
</Cards>
